# Optional: Supabase authentication. If both vars are set, the app
# gates behind a sign-in screen (email/password + magic link + Google
# + Apple). If unset, the app runs auth-free and projects live in
# localStorage. See README.md → "Authentication (optional)".

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
